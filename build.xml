<!--
    Copyright (c) Janne Jalkanen 2001, Canadensys 2011
    Based on http://www.jspwiki.org/wiki/SampleAntBuildFile
-->

<project name="dwca2sql" default="jar" basedir=".">
	
	<!-- The class files are actually put in this directory-->
	<property name="code.build" value="build" />
	
	<property name="lib.dir" value="lib" />
	<property name="file.name" value="dwca2sql.jar"/>

	<!-- ============================================================== -->
	<!-- Initialising, cleaning, etc. -->
	<target name="init" depends="clean" description="Creates directories">
		<mkdir dir="${code.build}" />
	</target>

	<target name="clean" description="Removes all generated files.">
		<delete dir="${code.build}" />
	</target>
	
	<path id="compile.classpath">
		<fileset dir="${lib.dir}">
			<include name="*.jar"/>
		</fileset> 
	</path>
	
	<!-- ============================================================== -->
	<!-- Compiling -->
	<target name="compile" depends="init" >
		<javac destdir="${code.build}" debug="true" srcdir="src">
			<classpath refid="compile.classpath"/>
		</javac>
	</target>
	

	<!-- ============================================================== -->
	<!-- Build JAR target -->
	
	<target name="jar" depends="compile">
   	<manifestclasspath property="lib.list" jarfile="${code.build}/${file.name}">
	    <classpath refid="compile.classpath"/>
	   </manifestclasspath>
   
		<jar destfile="${code.build}/${file.name}" basedir="${code.build}">
			<manifest>         
	            <attribute name="Main-Class" value="net.canadensys.dwca2sql.Dwca2SQLMain"/>
				<attribute name="Class-Path" value="${lib.list}"/>
			</manifest>
		</jar>
		<copy todir="${code.build}/${lib.dir}">
			<fileset dir ="${lib.dir}"/>
		</copy>
		<!-- Delete the files used to build this jar -->
		<delete includeemptydirs="true">
			<fileset dir="${code.build}">
				<exclude name="${file.name}"/>
				<exclude name="${lib.dir}/*"/>
			</fileset>
		</delete>
		<echo>${file.name} ready</echo>
	</target>
</project>